~~~ad-info


1. **分享创建模块**
   分享选项设置
     有效期选择：1天、7天、30天、永久
     提取码设置：可选是否随机生成提取码
     分享链接生成
     唯一分识符生成（UUID）
     包含有效期和提取码信息
2. **分享访问验证模块**
   用户身份验证
     检查访问者是否已登录系统
     未登录用户引导至登录页面
     分享有效性检查
     验证分享链接是否存在且未被删除
     检查分享是否在有效期内
     验证提取码（如设置）
     记录访问日志
3. **文件展示模块**
   文件信息展示
     显示分享者信息
     展示文件基本信息（名称、大小、类型等）
     显示分享创建时间和过期时间
     文件预览
     支持文档、图片、代码等文件类型的在线预览
     对于不支持预览的文件类型提供下载选项
4. **文件操作模块**
   选择文件保存到个人库
     选择直接下载文件到本地
5. **数据模型设计**
   ShareRecord（分享记录）
     id: 主键
     shareId: 分享唯一标识符（URL参数）
     fileId: 被分享的文件ID
     creatorId: 分享创建者ID
     expireTime: 过期时间
     extractCode: 提取码（可为空）
     createTime: 创建时间
     status: 分享状态（有效/无效/已删除）
6. **用户界面设计**
   **分享设置弹窗**
     有效期多选一
     提取码开关及生成按钮
     分享链接及提取码显示区域
     复制链接按钮
     **分享访问页面**
     登录验证区域（未登录时显示）
     提取码输入框（需要时显示）
     文件信息展示区域
     文件操作按钮（保存、导出)
     文件预览区域
     **安全考虑**
     分享链接应具备足够的随机性防止枚举攻击
     提取码应具有足够复杂度(6位字母+特殊符号+数字组合)
     访问日志记录便于追踪
     过期分享自动清理机制


~~~



---

**优化：**
```ad-success
**git结合ai 可以让他分析之前提交的情况和当前的情况 
写代码前特别是大的模块，一定要有方向和具体策略以及约束要求深刻理解需求和业务流程，而不是直接交给ai自由发挥 有问题要及时叫停  | ai生成执行任务计划 |然后多个分支并行开发 或者看看|云端开发什么东西Wegent**
```

# 需求阶段
总之一切都要按照当前项目场景的上下文（短 中 长期目标）

**关键词：成熟方案，拓展功能描述**

请你基于我目前的需求给我优化需求描述（**看看有什么不足和漏洞**） 后 帮我设计原型界面 两者迭代确认  帮我优化提示词



为文档管理平台构建「批量文档链接分享体系」，支持用户一次性选择多个文档生成专属分享链接，通过提取码验证、时效管控保障分享安全，同时实现接收方从链接校验到文档一键导入个人库的全流程闭环，核心体验对标主流网盘的高效性与安全性，满足个人协作、跨团队传递等高频文档分发场景。

```ad-warning
根据需求prompt优化后 应该要自己理清业务流程 就是需求中的执行链路分析 分析没问题后进入下一步（而不是直接将prompt直接交给ai 每一个状态结点都需要审查review 使用 代码审查提示词）

```

### 分享方操作流程（核心能力）

#### （1）批量文档选择与前置校验

- 选择方式：支持列表/网格视图下「复选框勾选」「全选/反选」「拖拽框选」（仅网格视图）三种模式，适配不同操作习惯；
- 选择反馈：选中文档实时高亮，底部操作栏同步显示「已选数量/单次上限」（建议上限50个，避免性能问题），hover单条选中文档时显示「取消选中」独立按钮；
- 前置校验：若选中文档包含「无分享权限文件」（如他人共享的只读文档、系统禁分享文件），点击「分享」后弹窗提示，列出无权限文件名称及原因，提供「跳过无权限文件继续分享」「取消操作重新选择」两种选项，避免流程中断；
- 特殊场景：选中单个文档时功能通用，批量与单个操作入口统一，降低学习成本。

#### （2）分享链接配置（安全与灵活双维度）

- 提取码设置：默认「开启提取码」，系统自动生成6位数字字母组合码，支持用户手动修改（4-8位，支持数字+字母），提供「隐藏提取码」选项（适用于内部信任场景）；

- 时效控制（成熟方案适配）：提供「短期时效」（1天/3天/7天/30天）、「自定义日期」（选择具体失效时间点，精确到小时）、「永久有效」三种选项，默认选中「7天有效」，时效到期前24小时，向分享方发送「分享即将失效」站内信提醒；

  

#### （3）分享结果与管理

- 成功反馈：生成「分享完成」弹窗，展示链接、提取码（若有）、失效时间，提供「复制链接+提取码」

### 接收方操作流程（闭环体验）

#### （1）链接访问与多层校验

- 身份校验：点击链接后，系统先判断用户状态——未登录则跳转至「登录/注册页」，登录后返回链接校验流程；已登录则进入下一步；
- 提取码校验：若分享时设置提取码，展示提取码输入框，支持「显示密码」，输入错误时提示「提取码不正确，请重新输入」（错误3次后，5分钟内限制再次尝试）；
- 时效校验：提取码验证通过后，判断链接是否过期——过期则展示「该分享已过期，可联系分享方重新分享」；有效则进入文档预览与导入页面。

#### （2）文档预览与一键导入

- 预览能力：支持在线预览常见格式文档（doc/docx/pdf/txt/xlsx等），预览页显示「文档名称、大小、分享方昵称、剩余有效时间」；
- 导入路径选择：提供「选择导入路径」按钮，点击弹出「我的文档」路径树，支持「新建文件夹」「选择已有文件夹」，默认选中根路径「我的文档」；
- 导入操作：支持「全部导入」「勾选部分文档导入」，点击导入后显示进度条，导入完成提示「成功导入X个文档，可前往[我的文档-路径]查看」；
- 重复处理：若导入路径中存在同名文档，弹窗提示「文档[名称]已存在，是否覆盖？」，提供「覆盖」「保留原文件（自动重命名新文件）」「取消导入该文件」选项。



### 异常场景与安全保障（补充漏洞点）

- 链接安全：分享链接采用「用户ID+文档ID+随机字符串」的加密规则




# 数据库表设计阶段

- 梳理业务流程图/接口分析（如有 也可以迭代分析），识别核心业务对象（==实体对象分析==）
- 明确数据量级预估（日增量、总量、峰值QPS）
- 确认数据生命周期（热数据/冷数据/归档策略）

**关系判定：**
- **1:1** → 考虑合并或拆分（大字段、访问频率差异）
    
- **1:N** → 外键在"N"端
    
- **M:N** → 需要中间表

**范式校验**

| 范式       | 核心要求               | 违反示例                   | 修正方案                              |
| -------- | ------------------ | ---------------------- | --------------------------------- |
| **1NF**  | 字段原子性，不可再分         | `address: "北京市朝阳区xxx"` | 拆分为 province/city/district/detail |
| **2NF**  | 消除部分依赖（非主属性完全依赖主键） | 订单表含商品名称               | 商品名称移至商品表                         |
| **3NF**  | 消除传递依赖             | 订单表含用户手机号              | 通过 user_id 关联查询                   |
| **BCNF** | 消除主属性对候选键的部分/传递依赖  | 复合主键场景                 | 拆分表或调整主键                          |

**范式与反范式的权衡**：

 **适合反范式的场景**：

- ✅ 读多写少（如商品详情页）
    
- ✅ 关联数据不常变更（如订单快照用户昵称）
    
- ✅ 高频查询需要减少 JOIN
    
- ✅ 报表/统计场景
    

**不适合反范式的场景**：

- ❌ 数据频繁变更（需要同步更新多处）
    
- ❌ 存储空间敏感
    
- ❌ 数据一致性要求极高
#### 需要拆分的场景

|场景|判定依据|示例|
|---|---|---|
|**实体独立性**|实体有独立生命周期|用户表 vs 订单表|
|**访问频率差异**|冷热数据分离|用户基本信息 vs 用户扩展信息|
|**大字段隔离**|TEXT/BLOB 字段影响查询|文章表 vs 文章内容表|
|**M:N 关系**|多对多必须中间表|用户-角色关联表|
|**历史记录**|需要保留变更历史|订单状态变更日志表|

#### 可以合并的场景

|场景|判定依据|示例|
|---|---|---|
|**1:1 强关联**|总是一起查询|用户表含头像URL|
|**枚举/配置**|数据量小且稳定|状态码直接存字段|
|**性能优先**|减少JOIN提升查询|订单表冗余用户昵称|

**字段设计**
字段==类型选择==原则

字段==约束设计==原则

```sql
--  通用基础字段规范
CREATE TABLE xxx (
  -- 主键
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
  
  -- ... 业务字段 ...
  
  -- 审计字段
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  created_by BIGINT COMMENT '创建人ID',
  updated_by BIGINT COMMENT '更新人ID',
  
  -- 软删除
  is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记: 0=正常, 1=已删除',
  deleted_at TIMESTAMP NULL COMMENT '删除时间',
  
  -- 乐观锁（高并发场景）
  version INT NOT NULL DEFAULT 0 COMMENT '版本号'
);
```

**业务字段设计**
```css
每设计一个表，问自己：


□ 列表页需要展示什么？ → 基本信息字段
□ 详情页需要展示什么？ → 详细信息字段
□ 怎么搜索/筛选？ → 需要索引的字段
□ 怎么排序？ → 排序字段（可能需要冗余）
□ 有哪些状态？ → status 字段 + 状态时间字段
□ 谁创建/操作的？ → user_id, created_by
□ 什么时候发生的？ → 时间节点字段
□ 需要关联什么数据？ → 外键字段
□ 哪些关联数据要冗余？ → 冗余字段（快照/避免JOIN）
□ 需要统计什么？ → 计数字段（避免COUNT）
```

```css
业务流程 → 操作动作 → 数据需求 → 字段定义

字段不是凭空想的，而是从「业务流程 → 用户操作 → 数据需求」一步步推导出来的。每个字段都要能回答"这个字段在哪个场景下被使用"。
```

<mark style="background: #BBFABBA6;">以上分析的都是要循环迭代分析的 最重要的一环 时间可以多用一点！！！！
</mark>
---

关键词：表、字段名、字段类型、其余属性设置要合理，理清需求和原型当中的实体和关系，并且都要有为什么这么配置的原因，并且说明有无范式问题和性能潜在问题。优化拓展建议

根据以上需求的描述，为我构造数据库表的设计，并对每个字段的作用以及表的作用和关系都解释清楚（说明是什么作用！！！）可以先不要配置索引等优化处理

**分享主表（tb_document_share）**

| 字段名           | 字段类型     | 是否主键 | 是否非空 |
| :--------------- | :----------- | :------- | :------- |
| share_id         | BIGINT       | 是       | 是       |
| share_user_id    | BIGINT       | 否       | 是       |
| share_code       | VARCHAR(100) | 否       | 是       |
| extract_code     | VARCHAR(100) | 否       | 否       |
| valid_start_time | DATETIME     | 否       | 是       |
| valid_end_time   | DATETIME     | 否       | 是       |
| share_status     | TINYINT      | 否       | 是       |
| create_time      | DATETIME     | 否       | 是       |
| update_time      | DATETIME     | 否       | 是       |
| ext_fields       | VARCHAR(500) | 否       | 否       |

**分享文档关联表（tb_document_share_rel）**

| 字段名      | 字段类型     | 是否主键 | 是否非空 | 补充说明                             |
| ----------- | ------------ | -------- | -------- | ------------------------------------ |
| rel_id      | BIGINT       | 是       | 是       | 无调整                               |
| share_id    | BIGINT       | 否       | 是       | 无调整                               |
| res_id      | BIGINT       | 否       | 是       | 原`doc_id`更名，兼容文档 / 文件夹 ID |
| res_type    | TINYINT      | 否       | 是       | 资源类型（1 - 文档，2 - 文件夹）     |
| res_name    | VARCHAR(255) | 否       | 是       | 存储文档 / 文件夹名称                |
| doc_size    | BIGINT       | 否       | 是       | 仅文档有值，文件夹填 0               |
| doc_type    | VARCHAR(50)  | 否       | 否       | 仅文档有值，文件夹填 NULL            |
| create_time | DATETIME     | 否       | 是       | 无调整                               |



# 技术方案&系统集成阶段

**关键词 ：其他实现方案，拓展建议，潜在问题！**

需从**架构设计、核心功能实现、集成适配、安全保障、异常处理、性能与体验、运维监控**七大维度展开，确保功能闭环且符合生产级要求

具体要看系统的上下文

```ad-info
## 角色定位
你是一位务实的全栈工程师，擅长在现有项目基础上做增量开发，追求"够用就好"的工程哲学。

## 核心原则
1. **复用优先**：先看项目里有什么能复用的，不要重复造轮子
2. **从简实现**：MVP 先行，能跑通再说，不要过度设计
3. **渐进优化**：如果有多种优化方案，帮我就单独整理一个md文档标记
4. **贴合上下文**：基于当前项目的技术栈、目录结构、现有模块来设计

## 输出要求

### 1. 现有资源盘点（必须先做）
- 当前项目用了什么技术栈？
- 有哪些现成的模块/组件/工具函数可以复用？
- 数据库已有哪些表？能否复用或扩展？
- 有没有类似功能可以参考？

### 2. 最小可行方案（MVP）
- 核心流程怎么跑通？（只关注 happy path）
- 需要新增什么？（表/接口/组件，越少越好）
- 能复用什么？（列出具体文件路径）
- 实现步骤（按优先级排序）

### 3. 潜在问题 & TODO
用表格列出并整理到md文档中，不在 MVP 中实现：

| 问题/优化点 | 影响程度 | 建议方案 | 优先级 |
|-------------|----------|----------|--------|
| xxx | 低/中/高 | xxx | P1/P2/P3 |

### 4. 备选方案（可选）
如果有明显不同的实现路径，简要对比 并整理到md文档当中：
- 方案 A：xxx（优点/缺点）
- 方案 B：xxx（优点/缺点）
- 推荐：方案 X，因为 xxx

## 不要做的事
❌ 不要设计用不到的抽象层
❌ 不要引入新的技术栈（除非必要）
❌ 不要一开始就考虑高并发/分布式
❌ 不要写大段理论，直接给方案
❌ 不要脱离项目上下文泛泛而谈

```


# 测试阶段（可选）

需要生成 执行计划 和 需求 设计文档（模块结构 依赖、调用关系 流程（算法、执行链路） 数据库）系统集成适配（复用当前项目已经有的功能 无缝集成！！）
```ad-warning
根据上面的执行计划以及你生成的md文档


```

